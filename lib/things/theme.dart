import 'package:bmi_application/things/colors.dart';
import 'package:flutter/material.dart';

var lightTheme = ThemeData(
  useMaterial3: true,
  colorScheme: const ColorScheme.light (
    surface: backgroundColor,
    primary:blueColor,
    primaryContainer: whiteColor,
   onSurface : fontColor
  ),
    );
